# TRS系统-功能设计文档编写 更新日志

## V4.0 · 2026-09-20 · 三方糅合版

### 为什么有这一版

本 Skill 此前存在三个并行版本，版本号互相倒挂，无法按号判断新旧：

| 版本 | 位置 | 日期 | 状态 |
|------|------|------|------|
| v3.8 | GitHub 早期线 | 2026-04-11 | 模板 / 质控门 / 反模式最丰富 |
| v1.2 | GitHub `master` HEAD（`ab20d21`） | 2026-09-08 | 最新单体骨架，含 spec.md |
| V3.0 | 本地分层版 | 2026-09-20 | 双交付模式 / 六章制 / 客户终稿口径 |

**v3.8 / v1.2 / V3.0 三个旧版本号一律作废**，勿再按版本号判定权威。本版为唯一权威。

### 糅合取了什么（不是覆盖，是各取所长）

| 来源 | 取用的内容 |
|------|-----------|
| **v3.8** | 验收优先硬规则（每子功能 ≥3 条验收标准，追溯/召回必含"可演练、可导出、可审计"）；输出格式选择表（Markdown / Word 友好） |
| **v1.2** | 骨架主体：Iron Law / Core Workflow / 质控门 / 质控报告 / 写作工作流细则（Step 1-5）/ 反失败纠偏脚本 / 反模式 / Quick Reference / 章节写法示例；`spec.md` 的 ID 体系（F/AC/BR/NFR/RISK/ASSUME）与枚举值 |
| **V3.0（本地）** | 双交付模式（`INTERNAL` / `CLIENT-FINAL`）；CLIENT-FINAL 六章制与减法清单；客户终稿 12 列；代码驱动原型设计；客户改稿五条；内联纪律层 `trs-spec-style` / `trs-evidence` |

### 四处矛盾及裁决

| # | 矛盾 | 裁决 | 依据 |
|---|------|------|------|
| 1 | 章节编号：v1.2 的 1-11 体系 vs V3.0 的六章制 | **不是二选一，是同一体系的两个视图**：`INTERNAL` 用 1-11，`CLIENT-FINAL` 删 2/8/9/10/11 后重排 1-6。映射表见 §0 | 两者本就对应同一套章节，只是裁剪不同 |
| 2 | 数据项 12 列存在两套列名 | **按模式取用**：`CLIENT-FINAL` 用客户终稿 12 列（含报错文案 / 取值来源 / 变更记录），`INTERNAL` 用开发实现 12 列（含控件类型 / 长度 / 新增编辑可见性）。见附录 D §3 | 客户终稿面向业务验收，开发实现面向研发落地；2026-09-16 客户改稿实证 |
| 3 | 验收标准 / 成功指标 / 文档状态 | `CLIENT-FINAL` **一律删除**，内容降级到附录 E（`INTERNAL` 专用），一条不丢 | 减法清单 + 客户改稿实证 |
| 4 | 版本号 3.8 → 1.2 → 3.0 倒挂 | 统一重起 **v4.0** | — |

### 结构性修正（糅合过程中发现的缺陷）

1. **v1.2 标题名与内容不符**：v1.2 把「写作工作流细则（Step 1 最小输入集 / 假设表 / 最小问题集 / 反失败纠偏脚本 / 可交付最小标准）」放在标题为「原型理解摘要」的 H2 下。按标题名判断会漏读整块 273 行规则。本版按内容正名为「写作工作流细则（Step 1-5）与原型理解摘要模板」。
2. **补回 v3.8 独有、v1.2 与 V3.0 均缺失的输出格式选择表**：Markdown / Word 友好格式二选一，Word 友好时表格与层级从简，否则 pandoc 导出 `reference.docx` 后版式会崩。

### 对账结果（可复现）

以三方规范性句子为分母的自动对账（`工具/_merge_verify_20260920.py`）：

- v3.8：47 条规则 → 命中 41
- v1.2：46 条规则 → 命中 46
- V3.0：33 条规则 → 命中 33
- **合计 126 条，命中 120（95.2%），零真丢**

6 条未逐字命中，逐条判定如下（**均为改写 / 演进 / 有意废止，非丢失**）：

| 原句 | 判定 |
|------|------|
| 接口契约章节应避免原始 JSON 的引导语 | 被 v1.2 更完整的「接口契约写法规范」取代（附录 A §9） |
| 质控门「① 完整性」维度行 | 存在，表述改为 v1.2 口径 |
| `Step 0.5: 先收集最小输入集` | v1.2 已演进为 `Step 1`，语义保留 |
| TRS 领域补充「必须覆盖其一或明确不适用」 | V3.0 §9 硬约束 2 已按模式改写（CLIENT-FINAL 下直接不写该章） |
| 数据项必须使用 11 列 | **有意废止**，统一为 12 列（矛盾 2） |
| 最小问题集（追溯粒度 / 关键节点 / …） | 存在，仅引号字符差异导致逐字不匹配 |

### 文件结构

```
SKILL.md                     自包含单体（1337 行），不依赖任何外部 skill
reference/                   15 个参考文件（本地版覆盖同名，本地更新）
testing/pressure-scenarios.md
reference.docx               Word 导出模板
build_reference.js
.github/workflows/           npm 发布链路
```

> 本地另有一套**分层版**（`trs-prd-writer` 编排层 + `trs-spec-style` / `trs-evidence` 等纪律层 skill）。
> 本目录是**由分层版生成的单向快照**，不反向合并。改纪律改本地，再重新生成快照。

---

## 历史版本（已作废，仅存档）

- **v1.2**（2026-09-08）
  - 新建 `reference/spec.md` 单一事实源（章节编号 / 数据项列定义 / ID 体系 / 版本 / 枚举 + 冲突仲裁顺序）
  - 统一章节编号为 1-11 体系，修正 SKILL.md Step4 表与示例输出、README 章节表
  - 修正接口契约操作总览表列数（7 → 5），补 `7.5 接口契约（业务视角）` 小节
  - 数据项表格统一为 12 列标准列名，README 修正"11 列 / 11 属性"表述
  - 修正 Word 导出依赖：pandoc 为首选，`md-to-office` 降级为条件使用
  - examples.md 示例 1 补齐第 8/9 章，满足输出门禁

- **v3.8**（2026-04-11 线）：质控门 5 维度评分、反模式、压力测试场景、原型理解摘要。

- **V3.0**（2026-09-20，本地）：改为编排层，规格抽离至 `trs-spec-style`、取证委托 `trs-evidence`、INTERNAL 专属条款下推 `reference/internal-mode.md`。

- **V1.1**（2026-05-06）：优化流程图语法（Mermaid）、修复列头/编号问题、重构输出管线。

- **V1.0**（2026-04-11）：首个对外发布版本，涵盖功能设计文档编写的完整方法论。
