---
name: trs-prd-writer
description: "TRS（晶科储能追溯系统）功能设计文档写作的自包含发布版。覆盖：定交付模式（INTERNAL / CLIENT-FINAL）→ 选输出目标（Markdown / 本地 docx / 钉钉云文档）→ 收最小输入集 → 需求挖掘 → 逐章编写 → 质量检查。含文档规格（六章制 / 减法清单 / 数据项 12 列 / 校验 3 要素）与取证口径（证据源五级分级 / 死组件三判据）。当用户要编写或改写追溯系统的功能设计文档时使用。不用于：成稿对抗性审查、查整块漏写、产出可运行的前端原型代码。"
version: 4.1
---


> # 本文件是**发布投影，不是权威**
>
> - **唯一权威（SSOT，可写）**：本地分层体系
>   `trs-prd-writer`（编排层）+ `trs-spec-style` / `trs-evidence` / `trs-truth` 等纪律层。
> - **本文件由 `工具/build_publish_skill.py` 自动生成，禁止手改** —— 手改即二次分叉。
> - 生成时间：2026-09-21　版本：4.1
>
> 附录 A / B 是纪律层的**内联副本**，仅为离线自包含；与 SSOT 冲突时**一律以 SSOT 为准**。

# trs-prd-writer · 写作编排

## 0. 权威与依赖

**本 skill 是 TRS 功能设计文档写作的唯一权威。** 已废弃 `writing-functional-specs`（其强制输出的 Out of Scope / 假设表等与本项目客户终稿要求相反），禁止再用。
**版本号高 ≠ 权威**：废弃副本版本号（3.2）高于本 skill，不看版本号判定权威。

裁决顺序：**用户裁决 > 本 skill 条款 > 其他写作 skill**。用户说"不用写 / 删掉 / 太啰嗦"一律遵从。

**本 skill 只管流程。** 两件事不在本文件：

| 问的是 | 走 |
|---|---|
| 这一节该长什么样 / 这个词能不能写 | `trs-spec-style` |
| 这个值 / 这个状态 / 这句报错的真值是什么 | `trs-evidence` |

**动手前必做**：先加载项目级 skill，其优先级高于本 skill 任何条款。

```bash
ls .workbuddy-ai/skills/ 2>/dev/null; ls .workbuddy/skills/ 2>/dev/null
```

命中则全部读取并遵守（典型：`trs-project-rules`）。未执行此步即动笔 = 违规。

## 1. Step 0 — 定交付模式

| 模式 | 交付对象 | 触发语 |
|---|---|---|
| `INTERNAL` | 团队评审、自检、过程推演 | 未说明、"内部评审"、"草稿"、"先写一版" |
| `CLIENT-FINAL` | 发给甲方的正式交付物 | "给客户"、"终稿"、"正式版"、"发给甲方"、"交付版" |

- 章节范围与减法清单 → `trs-spec-style` §1-§3
- `INTERNAL` 专属条款（Output Contract / 硬约束 5 条 / 反失败纠偏脚本 / 范围裁剪）→ `reference/internal-mode.md`
- **输出目标（Markdown / 本地 docx / 钉钉云文档）—— 与交付模式同一决策点，动笔前必须定** → `reference/output-targets.md`

## 2. Step 1 — 收最小输入集

优先收集；缺少则按模式处理（`CLIENT-FINAL` 下不写假设表，改为按 `trs-evidence` 去代码取真值）：

- **业务与边界**：业务域 / 工厂 / 组织范围、产品物料范围、追溯粒度（批次/箱/件/序列号）、时间范围
- **主链路**：正向/反向追溯链路、关键节点（采购/入库/生产/检验/包装/出库/客户）
- **数据与编码**：批次/条码/序列号规则、标签打印与贴标规则、扫码采集点位与频率
- **系统集成**：ERP/MES/WMS/QMS/PLM 的主数据与事件来源、接口方式、责任方
- **合规与审计**：审计留痕要求、记录保留年限、数据更正机制、权限与职责分离
- **目标与指标**：召回演练时限、准确率/覆盖率/时效

## 3. Step 2 — 识别工作模式

| 条件 | 走 |
|---|---|
| 用户提供了 HTML / 图片 / Figma 原型 | **原型驱动** → `reference/prototype-quickstart.md` |
| 手上有可运行的真实代码（前后端仓库） | **代码驱动** → `reference/code-driven-prototype.md` |
| 用户口头描述需求 | 传统需求沟通 → `reference/requirements-discovery.md` |
| 用户问"PRD 模板怎么写" | 传统需求沟通 |

三种模式可以接力：代码驱动先反推出界面骨架，原型驱动再把它做成可评审的原型。**有代码时优先代码驱动** —— 代码是事实，原型只是呈现。

## 4. Step 3 — 需求挖掘

5W1H 做问题定义与价值验证；TRS 领域补充项（谱系 / 召回 / 放行 / 条码 / 审计 / 集成）；字段设计询问框架。
→ `reference/requirements-discovery.md`（含**数据项 12 列规范**、字段类型中文命名表、界面流转图规范、Word 导出字号与表格规范）

## 5. Step 4 — 逐章编写

按 `trs-spec-style` 的章节结构逐章写。`CLIENT-FINAL` 下删除 2 / 8 / 9 / 10 / 11 后**连续重排为 1-6**，不得跳号。
各章节填写说明 → `reference/document-structure.md`

**取值 / 状态 / 报错文案一律现查**：写完一章，用 `trs-evidence` 的五级分级核一遍这一章出现的所有取值。

## 6. Step 5 — 质量检查（三段式）

```
1. 结构检查 → 章节完整、编号连续
2. 内容检查 → 字段完整、校验明确
3. 细节检查 → 报错文案、术语一致
```

- 检查清单 → `CHECKLIST_V2.0.md`
- 追溯领域模板 → `reference/traceability-domain.md`
- 自检质控门（5 维度评分，**仅 INTERNAL**，结论只在对话中输出） → `reference/quality-gate.md`

## 7. 输出格式与导出

**输出目标已在 Step 0 选定**，本节只给通道与验收动作 → `reference/output-targets.md`

| 目标 | 通道 | 验收动作 |
|---|---|---|
| Markdown | 默认 | 表格渲染正常、层级无跳号 |
| 本地 docx | pandoc + 自带 `reference.docx`（微软雅黑 / 全包框线） | 转出后打开看版式：表格未错位、目录正确 |
| 钉钉云文档 | `dingtalk-doc-writeback`（调用纪律走 `dingtalk-doc-calling`） | 块级回读：表头加粗 / 底色 / 列宽在、列表块未丢 |

> pandoc 命令、Word 友好格式对照、钉钉写作期约束 → 一律见 `reference/output-targets.md`（单点归位，不在本节重复）。

## 8. 资源索引

| 需要 | 文件 |
|---|---|
| 输出目标三选一（写作期约束） | `reference/output-targets.md` |
| 写作纪律（Iron Law / Bottom Line） | `reference/writing-discipline.md` |
| 规格定义 SSOT | `reference/spec.md` |
| 原型驱动详细指南 | `reference/prototype-driven.md` |
| 需求挖掘 + 12 列 + 字段命名 | `reference/requirements-discovery.md` |
| INTERNAL 专属条款 | `reference/internal-mode.md` |
| 自检质控门 | `reference/quality-gate.md` |
| 反模式 8 条 | `reference/anti-patterns.md` |
| 输出示例 | `reference/output-sample.md` |
| 模板库 / 工具集 | `reference/templates.md` / `reference/tools.md` |
| 章节详情 | `reference/document-structure.md` |
| 追溯领域模板 | `reference/traceability-domain.md` |
| 压力测试场景 | `testing/pressure-scenarios.md` |

## 9. 硬约束（两条，跨模式）

1. **值列表与校验不允许编造**：未明确时按模式处理，`CLIENT-FINAL` 下去代码取真值，不得用泛化经验代替。
2. **追溯领域补充项必须覆盖其一**：`追溯谱系` / `召回与演练` / `质量放行冻结` / `条码标签` / `审计留痕` / `集成与一致性` 至少命中 1 项。
   `CLIENT-FINAL` 下若不涉及 → **直接不写该章节**，禁止写"本功能不涉及 XX，原因是……"这类不适用声明。

---

**版本** 3.0（2026-09-20 体系重构）
**v3.0 说明**：按 mattpocock 分层原则改为编排层 —— 规格抽离至 `trs-spec-style`（SSOT）、取证委托 `trs-evidence`、`INTERNAL` 专属条款下推 `reference/internal-mode.md`。主干 499 → 本版。拆分前原文存 `_bak_20260920/SKILL.md`（807 行基线）。
**⚠️ v3.0 无零丢失证明**（2026-09-21 第三方审查裁定）：807→499 有 `工具/_verify_prd_split.py` 逐字验证；**499→126 这一步无验证脚本**，实测真丢 4 块 —— L1 Iron Law 借口表 / L2 动笔前问输出格式+Word 崩版警告 / L3 减法清单「为什么有害」列 + 新开发五条「反例（禁）」列 / L4 The Bottom Line。回灌动作见 `D:\功能设计完善\整改行动计划_TRSpredskill_20260921.md` 批次 1。
**v2.2 说明（历史）**：主文件由 807 行拆至 499 行以满足 500 行上限，6 个整块迁入 `reference/`。


---

# 附录 A · 文档规格（内联 `trs-spec-style`，权威在本地）

# trs-spec-style · 文档规格

## 1. 双交付模式

| 模式 | 交付对象 | 章节范围 |
|---|---|---|
| `INTERNAL` | 团队评审、自检 | 完整 1-11 章，含假设表 / 门禁 / 质控报告 / 非功能 / 发布计划 |
| `CLIENT-FINAL` | 发给甲方的正式交付物 | **仅六章功能设计本体** |

未说明时按 `INTERNAL`；出现"给客户 / 终稿 / 正式版 / 交付版"即切 `CLIENT-FINAL`。

## 2. CLIENT-FINAL 六章制（唯一允许出现的章节）

```
1 文档控制        → 1.1 更改记录（日期/作者/版本/一句话）
2 前言            → 2.1 说明（功能定位一句 + 需求来源一句）
3 问题定义与用户画像 → 3.1 核心问题陈述 / 3.2 用户画像 / 3.3 使用场景分析
4 概述            → 4.1 业务背景 / 4.2 概念说明 / 4.3 核心问题 /
                    4.4 开发目的 / 4.5 使用说明 / 4.6 范围【仅 In Scope】
5 总体设计        → 5.1 功能总体说明 / 5.2 界面流转 / 5.3 用户流程图 /
                    5.4 功能列表汇总 / 5.5 功能依赖说明
6 详细设计        → 每功能：6.x.1 界面设计及交互 / 6.x.2 核心逻辑 / 6.x.3 数据项设计（12列）
                    公共：值列表说明 / 交互说明 / 补充逻辑 / 接口契约（业务视角）
```

删除 2 / 8 / 9 / 10 / 11 后，剩余章节**连续重排为 1-6**，编号逐位相接。

## 3. 减法清单（CLIENT-FINAL 一律删除，无需再问）

| 一律删除的内容 | 归类 | 为什么有害 |
|---|---|---|
| Out of Scope / 范围外 / Non-goals | 边界防御 | 主动声明"不做什么" = 与客户划界限 |
| 假设与待确认表 | 不确定性暴露 | "你自己都没确定就给我了？" |
| 输入门禁确认 | 内部流程 | 属写作者自检动作，非功能内容 |
| 非功能性需求（性能/安全/审计/兼容/监控与日志） | 技术设计越界 | 应由技术设计文档承载 |
| 发布计划 / 成功度量 / 上线 / 试点 / 回滚 / 培训 | 项目管理 | 不是功能设计 |
| 成功指标 / SMART 指标表 / 检查清单 | 度量管理 | 同上 |
| 质控报告 / 干系人 / 决策记录 / 变更历史 | 内部治理 | 内部资料不应外发 |
| 未解决问题 / 已解决问题 | 问题跟踪 | 暴露未完成项 |
| 参与人员 / 审核 / 分发 / 文档状态空表 | 流程占位 | 空表 = 未完成感 |
| 手动目录 / 数据建模设计 | 冗余越界 | 云端自带大纲；建模属技术设计 |
| 参考文档节（内部路径 / 节点 ID / Skill 元信息） | 内部痕迹 | 暴露内部协作方式 |
| "规划中 / 待实现 / 未实现 / 代码尚未实现 / 后端未强制 / 问题清单命中 N 条" | 实现差距 | 直接暴露需求与实现的落差，对乙方不利 |
| 验收标准 / Given-When-Then / 验收用例 | 质量验证越界 | 客户终稿以功能描述 + 业务校验规则承载可验证性；验收条目属内部测试设计，外发暴露乙方测试口径 |
| 更改记录里的「移除 / 收敛 / 删减 XX 内容」表述 | 自证缺口 | 等于向客户宣告上一版写错了。删除过程只进内部台账，客户文档只留正向描述 |
| 为新开发功能编造的「历史痛点」与量化基线（现状耗时 X 分钟 / 错误率 Y%） | 无据臆造 | 本期新开发的功能本无历史系统与存量数据，编出来的痛点和基线一被追问就露馅 |

**判断口诀**：写"做什么"，不写"不做什么"；写"确定"，不写"待定"；写"功能"，不写"项目"与"过程"。

**成稿自检词表（命中即删）**：`范围外 / Out of Scope / 假设 / 待确认 / 门禁 / 非功能 / 性能 / 监控 / 日志 / 发布 / 上线 / 试点 / 回滚 / 成功指标 / 度量 / 干系人 / 决策记录 / 变更历史 / 质控 / 参与人员 / 审核 / 分发 / 未解决问题 / 验收 / Given / When / Then`

**更改记录 / 范围描述专用自检词**：`移除 / 收敛 / 删减 / 删除 / 原有 / 旧版 / 历史遗留` —— 出现即改写为正向描述。

> **改写法，不改行数**：「不写移除 / 收敛 / 删掉 XX」指**改写该行表述**，不是删除该行。
> §1.1 更改记录的历史行一律完整保留 —— 它是需求存在的唯一书证。

## 4. 新开发功能五条（2026-09-16 客户亲手改稿回灌）

本项目多数功能为本期新开发：无历史系统、无存量用户、无可比基线。据此定五条：

| # | 规则 | 反例（禁） | 正例 |
|---|---|---|---|
| 1 | **不臆造历史痛点**：§3.1 / §4.3 只写 1 句「缺什么」，**不写** `痛点 × 影响 × 优先级` 三列表 | 自造 3 行痛点表（「查询结果缺少层级化呈现 / 各环节数据分散在不同页签 …」） | 一句话：`缺少一个统一的界面进行内部追溯数据的查询` |
| 2 | **不编造量化基线**：无真实统计数据支撑，就不写「现状耗时 10 分钟 / 错误率 20%」 | 「手工记录每天需 2-3 小时整理文件」 | 只陈述能力缺口；量化仅用于**确有真实统计来源**的模块 |
| 3 | **更改记录只写「做了什么」**：不写「移除 / 收敛 / 删掉 XX」 | `1.2 收敛模块范围：移除内部任务详情、文件收集与审核 …` | 只保留正向描述（「补充各页签字段口径」「完善多方贸易数据展示」）；删除过程只进内部台账 |
| 4 | **同一口径只写一处**：核心逻辑不重复数据项表 / 补充逻辑已述的取值口径 | 6.1.2 再抄一遍「维度取追溯维度取值、具体值取 SN」 | 口径只在 6.x.3 数据项表与「补充逻辑」各写一次，其余引用不重述 |
| 5 | **不暴露内部码值 / 枚举代号**：一律用业务可读标识 | 「追溯类型为 **NC22** 或来源为海外仓时视为涉及多方贸易」 | 「追溯源为海外仓时视为涉及多方贸易」 |

> **判定口诀：新开发 = 只写「缺什么」，不写「以前多痛」。**
> 只有**存在历史系统 + 有真实数据**时，才允许写痛点表与基线值。
> 本 skill 内 `reference/examples.md` 的旧示例（供应链文件管理、批次谱系）均属**有历史系统**那一类，**切勿照搬到新开发功能上**。

## 5. 数据项表 12 列

`字段名 / 字段标识 / 字段类型 / 是否必填 / 是否唯一 / 默认值 / 取值来源 / 展示形式 / 校验规则 / 报错文案 / 备注 / 变更记录`
完整定义与填写规则 → `reference/data-item-spec.md`。

## 6. 值列表 5 要素

| 要素 | 询问问题 |
|---|---|
| 取数来源 | 取哪个表 / 快码？ |
| 级联关系 | 选 A 后 B 取什么？ |
| 默认规则 | 单值是否默认带出？ |
| 展示要求 | 显示名称还是值？ |
| 支持搜索 | 需要模糊搜索？ |

## 7. 校验 3 要素 与 报错文案

| 要素 | 示例 |
|---|---|
| 时机 | 保存时 / 实时 / 关闭时 |
| 条件 | XX 字段不允许为空 |
| 报错 | 「[XX]不能为空，请填写后保存」 |

- ✅ 「[文件名称]不能为空，请填写后保存」 「[编码]已存在，请重新命名」
- ❌ 「请填写完整信息」 「数据重复」 「操作失败」 「请检查输入」

报错一律**字段级**：字段名 + 修改建议。取真实文案时走 `trs-evidence` 的 L3 级（前端 `ElMessage` / 后端校验返回串，逐字比对）。

## 8. 多套状态机写法

一个业务对象常有多套并存的状态机。**只写一套 = 最大的漏写源**（本项目追溯任务有 6 套，文档原本只写 1 套）。

1. **逐套独立成节 / 成表**，命名带对象：`主任务状态` / `内部任务状态` / `外部任务状态` / `PPT报告任务状态` / `文件收集状态` / `下载状态`。一张表只承载一套状态机。
2. **状态流转单独写**，写明哪些状态可触发后续动作。
3. **同名词不同粒度 → 各自保留 + 加一句粒度说明**，不强行统一术语。
   （「审核拒绝」是文件级、「审核不通过」是任务级，两者都对。）
4. **真值怎么查** → 走 `trs-evidence` §3（Java 枚举 → 快码行按套比 → 最后看前端文案；已删除的快码行不写）。

## 9. 接口契约写法（业务视角）

1. **操作总览表 5 列**：序号 / 操作名称 / 触发时机 / 业务含义 / 业务校验规则
2. **每个操作**：用途说明 + 字段含义表（不用 JSON）+ 业务校验规则（自然语言）
3. **不展示**：HTTP 方法、URL 路径、原始 JSON、技术响应码、数据库表结构

技术实现（路径设计、参数类型、错误码、数据库方案）属技术设计文档，不在本文档范畴。

## 10. 字段类型中文命名

`文本 / 长文本 / 数字 / 小数 / 日期 / 日期时间 / 布尔 / 下拉单选 / 下拉多选 / 枚举 / 附件 / 图片`
完整对照与新增/编辑列填写规则 → `reference/data-item-spec.md`。

## 11. 依赖

- `trs-evidence` —— 取值 / 状态 / 报错文案的取证口径

本 skill 是纪律层，只管**规格**，不管流程；流程编排属编排层，不在此引用。


---

# 附录 B · 取证口径（内联 `trs-evidence`，权威在本地）

# trs-evidence · 取证原语

一条功能声明的可信度，取决于它落在证据梯的哪一级。**分级**是本原语唯一的组织原则：级别越高，越接近事实。

## 1. 证据源五级分级

| 级 | 证据源 | 管什么 | 不认什么 |
|---|---|---|---|
| **L1** | 后端 Java 枚举 `*Enum.java` | 状态机、流转条件、业务判定 | 界面措辞 |
| **L2** | 快码 / 字典行（DB 导出） | 下拉取值、选项集合 | 业务规则 |
| **L3** | 后端 Service / Mapper XML | 过滤规则、取数口径（须先确认是否死 SQL） | 界面呈现 |
| **L4** | 真实生效前端（路由页 / 业务 import 的组件） | 控件、按钮、报错文案 | 业务规则 |
| **L5** | 前端中文文案 | 仅确认界面措辞 | **不作存在性证据** |

- 跨级冲突 **以高级为准**。低级别命中而高级别零命中，判该声明**无据**。
- 全局裁决顺序：**代码 > 原功能设计文档 > 问题清单**。
- 每条判定 **必须带 `文件:行号`**。没有坐标的判定不是判定。

## 2. 真实生效代码三判据（死组件识别）

一个前端组件只有在满足下列之一时才算**真实生效**，可作权威：

1. **路由引用**：被路由配置的 `component` 字段指向。
2. **业务 import**：被某个业务 `.vue` / `.ts` 显式 import 并在模板中渲染。
3. **非类型声明引用**：引用方不是 `types/components.d.ts` 这类全局自动类型声明。

仅被类型声明文件引用 = **死组件** = 未接线的遗留物 = 不作权威。

**边界（不可外推）**：三判据只用于判定 `components.d.ts` 自动声明生成的条目。
`views/` 下的页面**不适用** —— 菜单常由后端动态下发，静态引用数为 0 的页面照样是有正式文档的功能。
判一个页面废弃前，先确认同类"已知有文档"的模块是不是也是 0 引用。

TRS 前端的常规形态：列表页 `index.vue` + 路由详情页 `detail.vue` / `field.vue`，
复制 / SQL 配置 / 节点预览 / 删除 / 字段配置 都由这两个真实页面承载；
`components/` 下的同名子组件多为遗留，须先过三判据再引用。

## 3. 状态与枚举三层查法

按此顺序查，前两层才是真值：

1. **后端 Java 枚举类**（`*Enum.java`）
2. **快码 / 字典行**，三条纪律：
   - **按套比**：同一个 code 在不同套里含义不同（如 `waiting` 在主任务是「任务准备中」、在外部任务是「任务已发起」）。比对时先锁定是哪一套，禁止跨套比 code。
   - **先过滤已删除行**：存在整套装值已删除的情况（曾有一套 8 条全删），文档引用即死引用。
   - **关联字段是 `qc_code`**：快码行的关联字段是 `qc_code`，不是 `qc_id` / `header_id`。用错字段关联 = 结果全空。
3. **前端文案**：只用于确认界面措辞。

同一业务词在不同粒度上是两套枚举（如文件级与任务级），属正常现象，
判定时**标注粒度区分**，而不是强行统一术语。

## 4. 通读全文纪律

凡涉及字段数 / 按钮数 / 报错文案 / 校验规则的判定，
**通读目标文件全文**后再下结论。只读前半段会漏掉后续的表单项、操作列与 `rules`。

## 5. 输出格式

每条判定写成一行：

```
<声明摘要> | <判定> | <证据级> | <文件:行号> | <修正方向>
```

判定值取三态之一，语义见 `trs-claim` §2。
